"""Draft codes of Simpy."""

        for elem in self.values:
            if self.values == []:
                filled_with.append(fill)


            

        if self.values == fill:
            len(self.values) == number_of_vals_to_fill
            self.values.append(fill)

