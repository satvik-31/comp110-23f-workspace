"""Practicing CQ05 for the quiz."""

#Combining lists into a dictionary

def zip(x: list[str], y: list[int]) -> dict[str,int]:
    """Combines two lists into a dictionary."""
    idx: int = 0
    if x != y and (x() or y()):
        return {}
    else:
        x[idx] = y[idx]
        result: dict (x[idx] == y[idx])
    return result 
